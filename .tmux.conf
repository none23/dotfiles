set -g history-limit 10000
set -s escape-time 0
set -g status-interval 10

setw -g mouse off
bind-key m set-option -g mouse on \; display 'Mouse: ON'
bind-key M set-option -g mouse off \; display 'Mouse: OFF'

unbind C-b
set -g prefix C-a
bind C-a send-prefix
set-window-option -g mode-keys vi

unbind-key h
unbind-key j
unbind-key k
unbind-key l

bind-key -r h select-pane -L
bind-key -r j select-pane -D
bind-key -r k select-pane -U
bind-key -r l select-pane -R

bind-key -n M-h select-pane -L
bind-key -n M-j select-pane -D
bind-key -n M-k select-pane -U
bind-key -n M-l select-pane -R

bind-key H resize-pane -L 10
bind-key J resize-pane -D 10
bind-key K resize-pane -U 10
bind-key L resize-pane -R 10

bind-key \; command-prompt

set -g display-time 5000

set -g status-style             bg='#111111',fg='#de5e1e'
set -g message-style            bg='#de5e1e',fg='#111111'
set -g message-command-style    bg='#cede1e',fg='#111111'
set -g pane-border-style        bg='#000000',fg='#111111'
set -g pane-active-border-style bg='#000000',fg='#de5e1e'

# Plugin-Manager Plugins
set -g @plugin 'tmux-plugins/tpm'
set -g @plugin 'tmux-plugins/tmux-resurrect'

run "$XDG_CONFIG_HOME/tmux/plugins/tpm/tpm"
# source "$XDG_CONFIG_HOME/tmux/local.conf"

# vim:filetype=zsh:foldmethod=marker:foldlevel=0
